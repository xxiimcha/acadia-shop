<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#EDE7F6">

    <TextView
        android:id="@+id/tvPlaceholderBack"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentStart="true"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        android:text="[Back]"
        android:textSize="16sp"
        android:gravity="center"
        android:background="#CCCCCC"
        android:textColor="#000000" />

    <ImageView
        android:id="@+id/verificationImage"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_below="@id/tvPlaceholderBack"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"
        android:background="#CCCCCC"
        android:contentDescription="Verification Image" />

    <TextView
        android:id="@+id/tvInstruction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/verificationImage"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Take a selfie with your school ID."
        android:textSize="16sp"
        android:textColor="#000000" />

    <Button
        android:id="@+id/btnNext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvInstruction"
        android:layout_marginTop="32dp"
        android:text="Next"
        android:backgroundTint="#9575CD"
        android:textColor="#FFFFFF" />
</RelativeLayout>
